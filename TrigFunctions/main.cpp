#include "Program/Program.h"

// TODO Write angle, sin(angle), cos(angle) -> Don't use functions, use length (maybe divide by radius)
// TODO Draw angle (Circle filled)
// TODO Make FUNCTION_POINTS change with Function Type.

int main(void)
{
  Program program;
  program.Run();
  return 0;
}